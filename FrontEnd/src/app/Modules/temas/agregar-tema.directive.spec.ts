import { AgregarTemaDirective } from './agregar-tema.directive';

describe('AgregarTemaDirective', () => {
  it('should create an instance', () => {
    const directive = new AgregarTemaDirective();
    expect(directive).toBeTruthy();
  });
});
